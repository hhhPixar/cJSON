# cJSON 阅读指南（面向 C 基础一般的读者）

这份文档配合源码里的中文注释，帮你把这个项目“看懂”，而不是只学会调用 API。

官方英文说明在 `README.md`。这里按**学习路径**来写：先建立图像，再对照函数，最后自己动手。

---

## 1. 这个项目是干什么的

JSON 是一段**文本**，例如：

```json
{"name":"Tom","age":18,"tags":["c","json"]}
```

程序里不能直接 `json.age + 1`，必须先变成 C 能操作的东西。cJSON 做三件事：

| 方向 | API | 结果 |
|------|-----|------|
| 文本 → 内存树 | `cJSON_Parse` | 一棵由 `cJSON` 结点组成的树 |
| 读写树 | `Get` / `Add` / `Replace` / `Create` | 改数字、加字段、遍历数组 |
| 内存树 → 文本 | `cJSON_Print` | 再变成 JSON 字符串 |

核心只有两个文件：

- `cJSON.h`：类型、宏、对外函数（你调用的都在这里）
- `cJSON.c`：解析、打印、链表操作的实现

其它文件可以先不管：

- `cJSON_Utils.c`：JSON Pointer / Patch 等扩展
- `tests/`：单元测试
- `test.c`：官方示例程序

---

## 2. 你需要提前会的 C 知识

读这个库时反复出现下面这些，不会也没关系，对着例子看两遍就够。

**指针和结构体**  
`cJSON *item` 表示“指向一个结点的指针”。`item->type` 是访问结构体字段。`NULL` 表示“没有”。

**链表（不是 C 数组）**  
数组 `[1,2,3]` 在 cJSON 里**不是** `int a[3]`，而是三个结点用 `next` 串起来。访问第 n 个要从头走 n 步，所以是 O(n)。

**递归**  
解析 `{` 里面还有 `{` 时，函数会调用自己。嵌套太深会撑爆函数调用栈，所以有 `CJSON_NESTING_LIMIT`（默认 1000）。

**位运算**  
`type` 里同时存“是什么类型”和“额外标志”。判断数字用 `item->type & cJSON_Number`，不要写成 `==`，因为可能还带着 `cJSON_IsReference` 等位。

**谁分配谁释放**  
`malloc` 出来的内存必须 `free`。cJSON 里约定：

- `Parse` 出来的树 → `cJSON_Delete`
- `Print` 出来的字符串 → `cJSON_free`（或 `free`，若你没换过内存钩子）

忘了释放就是内存泄漏；释放两次就是崩溃。

---

## 3. 内存里长什么样（最重要的一张图）

每个 JSON 值都是一个 `cJSON`：

```
next / prev  —— 和兄弟结点连成双向链表
child        —— 指向第一个孩子（只有数组、对象有）
type         —— 类型 + 标志
valuestring  —— 字符串内容（type 是 String / Raw 时）
valuedouble / valueint —— 数字
string       —— 对象成员的键名（注意：不是 valuestring）
```

把这段 JSON：

```json
{"name":"Tom","age":18}
```

画成树：

```
根结点  type = Object
  |
  child
  v
[ string="name", type=String, valuestring="Tom" ]
  next <--> prev
[ string="age",  type=Number, valuedouble=18, next=NULL ]
```

第一个孩子的 `prev` 还会指向**最后一个**孩子。这是为了追加元素时不用从链表头走到尾。读代码时看到 `array->child->prev`，想的是“当前最后一个元素”，不是“第一个的前一个”。

数组和对象的存储方式完全一样，区别只有：

- 数组：`type = Array`，孩子一般没有 `string`
- 对象：`type = Object`，每个孩子都有键名 `string`

---

## 4. 建议阅读顺序

不要从上到下把 `cJSON.c` 三千行一次读完。按这个顺序，每次只盯一块：

### 第 1 步：头文件里的结构体

打开 `cJSON.h`，只看：

- 类型宏：`cJSON_False`、`cJSON_Number` 等
- `typedef struct cJSON { ... }`
- `cJSON_Parse` / `cJSON_Print` / `cJSON_Delete`

目标：能指着结构体说出“数字存在哪、键名存在哪”。

### 第 2 步：结点的生与死

在 `cJSON.c` 里找：

- `cJSON_New_Item`：分配一块全 0 的结点
- `cJSON_Delete`：释放孩子、字符串、自己，再沿 `next` 处理兄弟
- `cJSON_strdup`：拷贝一份 C 字符串

对照注释里的两个标志：`cJSON_IsReference`、`cJSON_StringIsConst`。不理解标志时，可以先假设“普通结点的字符串都是自己 malloc 的，Delete 都要 free”。

### 第 3 步：解析主路径（文本 → 树）

调用关系：

```
cJSON_Parse
  └─ cJSON_ParseWithOpts
       └─ cJSON_ParseWithLengthOpts     ← 真正入口：准备光标、分配根结点
            └─ parse_value              ← 看当前字符，分发
                 ├─ 看到 null/true/false → 直接改 type
                 ├─ 看到 "             → parse_string
                 ├─ 看到 - 或数字      → parse_number
                 ├─ 看到 [             → parse_array → 循环里再调 parse_value
                 └─ 看到 {             → parse_object → 先 parse_string 当键名，再 parse_value
```

`parse_buffer` 就是“读文件的光标”：`content` 是整段文本，`offset` 是读到第几个字节。宏 `buffer_at_offset(buffer)` 等于“当前字符的地址”。

读 `parse_array` 时跟着这段输入走一遍：`[1,2]`。在纸上记下每一步 `offset` 指向哪个字符、链表如何接上。这比通读函数更快。

### 第 4 步：打印主路径（树 → 文本）

```
cJSON_Print
  └─ print                         ← 先分配 256 字节缓冲
       └─ print_value              ← 按 type 分发
            ├─ null/true/false → strcpy
            ├─ Number          → print_number
            ├─ String          → print_string → print_string_ptr（加引号、转义）
            ├─ Array           → print_array
            └─ Object          → print_object
```

`ensure` 负责“缓冲区不够就扩容”。这是动态数组的典型写法：容量 `length`、已用 `offset`，不够就 `needed * 2`。

### 第 5 步：增删查

这些函数不碰 JSON 文本，只改链表：

| 你想做的事 | 函数 | 内部其实在干什么 |
|------------|------|------------------|
| 取数组第 i 个 | `cJSON_GetArrayItem` | 从 `child` 沿 `next` 走 i 步 |
| 按键名查找 | `cJSON_GetObjectItem` | 同样走链表，比较 `string` |
| 追加到数组 | `cJSON_AddItemToArray` | 接到 `child->prev` 后面 |
| 加到对象 | `cJSON_AddItemToObject` | 先设键名，再当数组成员追加 |
| 摘下来不释放 | `cJSON_DetachItemViaPointer` | 改前后结点的 next/prev |
| 删除 | `cJSON_DeleteItemFrom*` | Detach + Delete |

`cJSON_ArrayForEach` 是个 for 循环宏，用来遍历孩子。

---

## 5. 用最小例子把调用和结构对上

下面这段可以自己编译（和项目一样：把 `cJSON.c` 一起编进去）。

```c
#include <stdio.h>
#include "cJSON.h"

int main(void)
{
    const char *text = "{\"name\":\"Tom\",\"age\":18}";
    cJSON *root = cJSON_Parse(text);
    if (root == NULL)
    {
        printf("parse failed near: %s\n", cJSON_GetErrorPtr());
        return 1;
    }

    cJSON *name = cJSON_GetObjectItem(root, "name");
    cJSON *age  = cJSON_GetObjectItem(root, "age");
    printf("%s is %d\n", name->valuestring, age->valueint);

    /* 改树：年龄 +1，再加一个数组字段 */
    cJSON_SetNumberValue(age, age->valuedouble + 1);
    cJSON *tags = cJSON_AddArrayToObject(root, "tags");
    cJSON_AddItemToArray(tags, cJSON_CreateString("c"));

    char *printed = cJSON_Print(root);
    printf("%s\n", printed);

    cJSON_free(printed);   /* Print 的结果 */
    cJSON_Delete(root);    /* Parse 的结果 */
    return 0;
}
```

调试建议：在 `parse_value`、`parse_object` 入口下断点，看 `buffer_at_offset` 指向的字符，和 `item->type` 如何被填上。

Windows 下若用 MinGW，可参考 `Makefile`；最简单是：

```text
gcc -std=c89 -o demo demo.c cJSON.c
```

（Linux/macOS 可能还要加 `-lm`，因为用了数学库。）

---

## 6. 几个容易绊倒人的点

**`string` 和 `valuestring`**  
对象 `{"age":18}` 里，孩子结点的 `string` 是 `"age"`，数字在 `valuedouble`。只有 `"hello"` 这种字符串值才用 `valuestring`。

**`cJSON_GetObjectItem` 默认忽略大小写**  
查找 `"Name"` 也能命中 `"name"`。要严格匹配用 `cJSON_GetObjectItemCaseSensitive`。

**数字有两份**  
`valuedouble` 是准的；`valueint` 只是截断/饱和后的 int。超过 `INT_MAX` 时 `valueint` 会变成 `INT_MAX`，不要只用它做精确计算。

**`NaN` / 无穷大**  
不是合法 JSON。打印时会被写成 `null`。

**不要直接改 `valueint`**  
用宏 `cJSON_SetNumberValue`，它会同时改 double 和 int。

**引用结点**  
`cJSON_CreateStringReference` 等不会拷贝内存。`Delete` 树时不会释放那块字符串。适合指向只读字面量，避免一次多余拷贝；用错就会野指针或泄漏。

**线程**  
`cJSON_GetErrorPtr` 用了全局变量。多线程同时 Parse 会互相覆盖错误位置。结点本身只要你自己保证不同线程不改同一棵树，一般可以并行使用不同的树。

**嵌套深度**  
恶意或异常 JSON `[[[[...` 会递归。库用 `CJSON_NESTING_LIMIT` 拒绝过深输入。

---

## 7. 源码地图（按主题）

| 主题 | 主要符号 | 文件位置（大约） |
|------|----------|------------------|
| 对外 API、结构体 | `cJSON`、`cJSON_Parse` | `cJSON.h` |
| 内存钩子 | `internal_hooks`、`cJSON_InitHooks` | `cJSON.c` 前部 |
| 解析光标 | `parse_buffer`、`can_read` | `parse_number` 上方 |
| 解析分发 | `parse_value` | 搜这个名字 |
| 数组/对象链表如何建成 | `parse_array`、`parse_object` | 紧接 `parse_value` 之后 |
| 字符串转义 | `parse_string`、`utf16_literal_to_utf8` | 中部 |
| 打印缓冲扩容 | `printbuffer`、`ensure` | `parse_number` 之后 |
| 打印分发 | `print_value` | `parse_value` 旁边 |
| 链表追加/摘除 | `add_item_to_array`、`cJSON_DetachItemViaPointer` | 后部 |
| 手工造结点 | `cJSON_Create*` | 更后部 |

`cJSON_Utils.c` 是进阶内容（按路径取字段、JSON Patch），把上面吃透后再看。

---

## 8. 怎样算“看懂了”

你可以不问别人，自己回答这些问题：

1. `{"a":[1,2]}` 在内存里有几个 `cJSON` 结点？谁的 `child` 指向谁？
2. 为什么释放树时既要递归 `child`，又要循环 `next`？
3. `parse_object` 为什么先 `parse_string` 再把 `valuestring` 赋给 `string`？
4. `ensure` 在 `noalloc == true` 时为什么只能失败、不能扩容？
5. 往对象里 `AddItem` 和往数组里 `AddItem`，代码路径差在哪一步？

能讲清楚，这个项目的核心就算过关了。接下来可以读 `tests/parse_object.c` 一类测试，看作者用哪些输入覆盖边界（空对象、嵌套、错误 JSON）。
