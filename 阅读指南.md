# 用 cJSON 学 C：语法、指针、内存

这份指南配合 `cJSON.h` / `cJSON.c` 里的逐段注释。目标不是“会调 API”，而是把 **C 里该掌握的东西** 在真实项目里过一遍。

官方英文 API 说明仍在 `README.md`。

---

## 0. 怎么用这份材料

1. 先读本文 **第 1～3 节**（类型、指针、内存），对照 `cJSON.h` 里的 `struct cJSON`。
2. 需要一张“谁调用谁”的地图时，看 `源码问答.md` **第 0 节**（`cJSON.c` 函数调用图）。
3. 打开 `cJSON.c` **文件最开头的长注释**（语法总表），再按第 4 节的函数顺序往下读。
4. 每读一个函数，在纸上画指针：谁指向谁、谁该 `free`。
5. 最后用第 8 节的练习题自测。

不要试图一天读完三千行。每次只盯一个主题（例如“链表怎么接”）。

---

## 1. 这个项目在干什么

JSON 是**文本**。C 不能写 `json.age + 1`，必须先变成结构体。

| 方向 | 函数 | 你得到什么 |
|------|------|------------|
| 文本 → 树 | `cJSON_Parse` | 堆上的 `cJSON` 结点，用指针连成树 |
| 改树 | `Get` / `Add` / `Create` | 改字段、改 `next`/`child` |
| 树 → 文本 | `cJSON_Print` | 堆上的 `char*`，以 `'\0'` 结尾 |

核心文件：

- `cJSON.h`：类型、宏、函数**声明**
- `cJSON.c`：函数**定义**（真正的代码）

---

## 2. C 语法在本项目里出现的位置

下面每条都是“概念 → 到源码哪里看”。读注释时把这句话和代码对上。

### 2.1 预处理

| 语法 | 含义 | 去哪看 |
|------|------|--------|
| `#ifndef cJSON__h` | 头文件守卫，防止重复 include | `cJSON.h` 开头 |
| `#define cJSON_Number (1 << 3)` | 宏：编译前文本替换 | `cJSON.h` 类型宏 |
| `#include <string.h>` | 引入 `strlen`/`memcpy` 声明 | `cJSON.c` 前部 |
| `#ifdef _MSC_VER` | 条件编译：只有 MSVC 才编这段 | `cJSON.c` 的 `internal_malloc` |
| `#error` | 编译期失败 | `.h` 和 `.c` 版本检查 |

宏**不是变量**，没有地址，调试时也看不到“宏的值在内存里”。`cJSON_ArrayForEach` 会展开成 `for (...)`。

### 2.2 类型与 typedef

| 语法 | 含义 | 去哪看 |
|------|------|--------|
| `typedef int cJSON_bool` | 给 int 起别名，C89 没有 bool | `cJSON.h` |
| `typedef struct cJSON { } cJSON` | 结构体 + 短名 | `cJSON.h` |
| `size_t` | 无符号长度，`sizeof`/`strlen` 用它 | 几乎每个分配函数 |
| `unsigned char` | 原始字节，避免 signed char 为负 | `parse_buffer.content` |
| `const` | 不能通过这个入口修改 | 大量参数 `const cJSON *` |

### 2.3 指针（必须掌握）

把这句话背熟：**指针变量里存的是地址，不是对象本身。**

```
cJSON *p;     // p 是指针，占 4 或 8 字节
*p            // 去地址上取那个 cJSON
p->type       // 等价 (*p).type
p = NULL      // 不指向任何合法对象；此时 *p 会崩溃
```

| 写法 | 读法 | 去哪看 |
|------|------|--------|
| `T *p` | 指向 T | 到处都是 |
| `const T *p` | 不能改 `*p` | `cJSON_Print(const cJSON *item)` |
| `T * const p` | 不能改 p 指向哪 | 参数 `cJSON * const item` |
| `const T * const p` | 两个都不能改 | `GetObjectItem` 的参数 |
| `T **p` | 指向“一个 T* 变量” | `utf16` 的 `output_pointer`；`Parse` 的 `return_parse_end` |
| `p + n` | 跳过 n 个 T | `buffer_at_offset` = `content + offset` |
| `q - p` | 中间隔几个元素 | `parse_number` 里 `after_end - number_c_string` |
| `(T*)x` | 强制转换指针类型 | `malloc` 的返回值转成 `cJSON*` |
| `p->next = q` | 让这个结点的 next 指向 q | `parse_array`、`add_item_to_array` |

**指针的指针**（初学者最懵）：

函数若要修改调用者的 `char *end`，必须传入 `&end`，类型是 `char **`。函数里写 `*end_ptr = 新地址`。见 `cJSON_ParseWithOpts` 的 `return_parse_end`。

**数组下标就是指针**：`a[i]` 等于 `*(a+i)`。所以 `buffer_at_offset(b)[0]` 就是当前字符。

### 2.4 结构体与链表

`struct` 把若干字段打包。本库**不用 C 数组存 JSON 数组**，而用：

- `child`：第一个元素
- `next` / `prev`：左右兄弟

这叫**双向链表**。取第 n 个要从头走 n 步（`get_array_item`）。

特殊优化：第一个孩子的 `prev` 指向**最后一个**孩子，追加才能 O(1)。见 `add_item_to_array`。

对象和数组在内存里是同一种链表，区别只是对象的孩子多一个 `string`（键名）。

### 2.5 内存：栈、堆、所有权

| 在哪 | 谁分配 | 谁释放 | 例子 |
|------|--------|--------|------|
| 栈 | 编译器自动 | 函数返回自动没 | `parse_buffer buffer = {...}` |
| 堆 | `malloc` / `hooks->allocate` | 必须自己 `free` | 每个 `cJSON` 结点、`valuestring` |
| 静态区 | 程序启动 | 程序结束 | `static char version[15]`；`global_hooks` |

规则：

- `Parse` 的树 → `cJSON_Delete`
- `Print` 的字符串 → `cJSON_free`
- `Delete` 前先把 `item->next` 存到局部变量，因为马上 `free(item)`，之后不能再读 `item->...`

`cJSON_IsReference` / `cJSON_StringIsConst`：**借用**别人的内存，Delete 时不能 free。

### 2.6 字符串

C 没有 `string` 类型。`"Tom"` 是 `T o m \0` 四个字节。`char *` 只指向第一个。

- `strlen` 数到 `\0` 之前，不含 `\0`
- 拷贝必须把 `\0` 一起拷（`cJSON_strdup` 里 `+ sizeof("")`）
- 不能 `free("字面量")`，字面量在只读段

### 2.7 函数与函数指针

- `static` 函数：只在本 `.c` 可见（`parse_value`）
- 头文件里的声明：给别的 `.c` 用（`cJSON_Parse`）
- 函数指针：`void *(*allocate)(size_t)`，变量里存函数地址。`cJSON_InitHooks` 把 `malloc` 换成你的分配器。

### 2.8 控制流

| 语法 | 本库怎么用 |
|------|------------|
| `if` / `while` / `for` | 到处都是 |
| `do { } while` | `parse_array`：至少尝试一次元素 |
| `switch` | `parse`/`print` 按类型分发；`parse_string` 按转义字符 |
| `goto fail` | 出错后跳到统一清理（释放半成品）。C 里很常见 |
| 递归 | `parse_value` → `parse_array` → 再 `parse_value` |
| 三元 `a?b:c` | `cJSON_SetNumberValue` 宏 |
| 短路 `&&` `\|\|` | 宏 `can_read`：先判 `buffer != NULL` 才去读 `buffer->offset` |

### 2.9 位运算

`type` 一个 int 里同时存类型和标志。

- `1 << n` 左移：得到第 n 位为 1 的数
- `a \| b` 按位或：把标志打上去
- `a & b` 按位与：测试某位是否为 1（判断类型要用这个，不要 `==`）
- `a & ~mask` 清掉 mask 那些位
- `a & 0xFF` 只保留低 8 位（打印前去掉 IsReference）

### 2.10 强制转换与未定义行为

- `(cJSON*)malloc(...)`：`void*` 转成结构体指针
- `(int)double`：超出 `INT_MAX` 是未定义行为，所以先饱和（`parse_number`）
- `strcpy` 源和目标重叠是未定义行为（`cJSON_SetValuestring` 先查指针区间）
- 解引用 NULL、use-after-free、double free：本库用大量 `== NULL` 检查和 `ptr = NULL` 来防

---

## 3. 内存里的树（对着结构体看）

`{"name":"Tom","age":18}`：

```
根 r                 type = Object, child 指向 A
                     r 自己在堆上

A                    string  ------> 堆上 "name\0"
                     valuestring --> 堆上 "Tom\0"
                     next ---------> B
                     prev ---------> B   （头的 prev 指向尾，优化）

B                    string  ------> 堆上 "age\0"
                     valuedouble = 18
                     next = NULL
                     prev ---------> A
```

练习：数一数这里有几次 `malloc`？（根 + A + B + 三块字符串，至少 6 次。）`cJSON_Delete(r)` 必须把它们全部 `free`。

---

## 4. 源码阅读顺序（跟注释走）

按这个顺序打开 `cJSON.c`，每个函数开头都有针对语法的说明。

1. **`cJSON_New_Item` / `cJSON_Delete` / `cJSON_strdup`**  
   malloc、memset、sizeof、free 前保存 next、所有权。

2. **`case_insensitive_strcmp`**  
   `*p`、`p++`、C 字符串以 `\0` 结束。

3. **`internal_hooks` / `cJSON_InitHooks`**  
   函数指针赋值与调用。

4. **`parse_buffer` 和四个宏**  
   指针加法、宏要加括号、`&&` 短路。

5. **`parse_number`**  
   临时缓冲、`char**` 输出参数、指针相减。

6. **`cJSON_ParseWithLengthOpts`**  
   栈上结构体、`&buffer`、`const char **`、`goto fail`。

7. **`parse_value` / `parse_array` / `parse_object`**  
   递归、链表接法、键名从 `valuestring` 挪到 `string`（两个指针曾指向同一块，必须把其中一个改成 NULL）。

8. **`parse_string` / `utf16_literal_to_utf8`**  
   `*p++ = *q++`、`unsigned char **`、位运算拼 UTF-8。

9. **`printbuffer` / `ensure` / `print_value`**  
   动态数组（容量 vs 已用）、realloc 后旧指针失效。

10. **`add_item_to_array` / `cJSON_DetachItemViaPointer`**  
    双向链表插入、删除。

11. **`cJSON_CreateNumber` / `CreateString`**  
    手工造结点；失败路径要 Delete 已分配的半成品。

后面大量 `CreateIntArray`、`AddXxxToObject` 是同一模式的重复，看懂一个即可。

---

## 5. 最小例子

把 `cJSON.c` 和下面一起编译：

```c
#include <stdio.h>
#include "cJSON.h"

int main(void)
{
    const char *text = "{\"name\":\"Tom\",\"age\":18}";
    cJSON *root = cJSON_Parse(text);
    if (root == NULL)
    {
        printf("parse failed near: %s\n", cJSON_GetErrorPtr());
        return 1;
    }

    cJSON *name = cJSON_GetObjectItem(root, "name");
    cJSON *age  = cJSON_GetObjectItem(root, "age");
    printf("%s is %d\n", name->valuestring, age->valueint);

    cJSON_SetNumberValue(age, age->valuedouble + 1);
    cJSON *tags = cJSON_AddArrayToObject(root, "tags");
    cJSON_AddItemToArray(tags, cJSON_CreateString("c"));

    char *printed = cJSON_Print(root);
    printf("%s\n", printed);

    cJSON_free(printed);
    cJSON_Delete(root);
    return 0;
}
```

```text
gcc -std=c89 -o demo demo.c cJSON.c
```

Linux/macOS 可能要加 `-lm`。

调试：在 `parse_value`、`parse_object` 下断点，看 `buffer_at_offset` 指向的字符，以及 `item->child` / `->next` 何时被赋值。

---

## 6. 易错点

- **`string` 不是 `valuestring`**：键名 vs 字符串值。
- **判断类型用 `&` 不用 `==`**。
- **`GetObjectItem` 默认忽略大小写**。
- **数字看 `valuedouble`**；`valueint` 会饱和。
- **返回的 `valuestring` 仍属于结点**，不要自己 `free`，结点 Delete 后也不能再用。
- **`static` 局部数组可以返回地址**（`cJSON_Version`）；栈上数组不行。
- **全局 `global_error`**：多线程同时 Parse 会互相覆盖。

---

## 7. 怎样算掌握了

不看注释，能讲清楚：

1. `p->next = q` 改的是内存里哪几个字节？`p = q` 又改的是什么？
2. 为什么 `Delete` 必须先 `next = item->next` 再 `free(item)`？
3. `const char **` 和 `const char *` 差在哪？什么时候必须用两层指针？
4. `content + offset` 和 `content[offset]` 是什么关系？
5. 对象解析里 `string = valuestring; valuestring = NULL;` 少写第二句会怎样？
6. `ensure` 里 `realloc` 成功后，为什么旧的 `buffer` 指针不能再用？
7. `1 << 3`、`type & cJSON_Number`、`type | cJSON_IsReference` 各做什么？
8. 宏 `can_read` 为什么要把参数和整个表达式都加上括号？

全都能用自己的话答出来，C 的指针、结构体、内存、宏、位运算就算过关了。然后再去看 `tests/` 里的边界用例。
